# Exercise Sheet 1
## Submission Deadline 
Monday 06.11.2023 10am

### Task 1
Name and explan in one sentence 5 current uses of computer graphics.
(5 points)

### Task 2
In the Folder ``src`` you find the example shown in the tutorial. Open the ``01.html``, you should see a white triangle on your screen. Similar to the way the vertices were defined, create an array of the colors red (1,0,0), green (0,1,0) and blue (0,0,1) in the JS code.
* In the JS code create a buffer and assign the color array to it
* Create in variable ``in_color`` in the vertex shader
* Find the newly in the vertex shader created attribute location using getAttribLocation
* In the vertex shader, change the currently set to vec4(1.0, 1.0, 1.0, 1.0) output color to the input color.

Watch out: The input color is a vec3, and the output vec4. You can add an additional 1 add 4th component, similar to the position variable
(5 points)